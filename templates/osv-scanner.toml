# OSV-Scanner configuration for {{CRATE_NAME}}
# Generated by chain-guard — https://github.com/chain-guard/chain-guard
#
# If your project contains test fixtures, sample data, or documentation
# with package references from non-Rust ecosystems, those will trigger
# false positives in vulnerability scans. Add overrides below to ignore them.
#
# GOTCHA: osv-scanner.toml does NOT propagate to child directories.
# If you have fixtures in subdirectories, each needs its own copy.

# Uncomment the ecosystems that appear in your test fixtures / sample data:

# [[PackageOverrides]]
# ecosystem = "npm"
# ignore = true
# reason = "Test fixture data, not actual dependencies"

# [[PackageOverrides]]
# ecosystem = "PyPI"
# ignore = true
# reason = "Test fixture data, not actual dependencies"

# [[PackageOverrides]]
# ecosystem = "Maven"
# ignore = true
# reason = "Test fixture data, not actual dependencies"

# [[PackageOverrides]]
# ecosystem = "Go"
# ignore = true
# reason = "Test fixture data, not actual dependencies"

# [[PackageOverrides]]
# ecosystem = "NuGet"
# ignore = true
# reason = "Test fixture data, not actual dependencies"
